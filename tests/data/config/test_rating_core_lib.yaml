# @package _global_
core_lib:
  data:
    sqlalchemy:
      session:
        pool_recycle: 3600
        pool_pre_ping: false

      url:
        protocol: sqlite

      log_queries: false
      create_db: true
